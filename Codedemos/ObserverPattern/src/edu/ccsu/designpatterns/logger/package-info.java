/**
 * This package contains a logger that writes to a file any messages at is of higher severity than
 * the specified threshold. For demonstrating the Observer pattern the Logger is a subject that can
 * be observed. The ConsoleObserver can be added as an observer of the Logger to write to the
 * console when a log event occurs with severity at or greater than a specified threshold.
 */
package edu.ccsu.designpatterns.logger;
